// POST - /api/lift/request/
// Endpoint for the post request to request the elevator
export const ElevatorRequestEndpoint: string = "/api/lift/request/";

// GET - "/api/lift/status/"
// Endpoint for the GET request to return elevators status
export const ElevatorStatusEndpoint: string = "/api/lift/status/";

// GET - "/api/lift/config/"
// Endpoint for the GET request to return elevators status
export const ElevatorConfigEndpoint: string = "/api/lift/config/";
